import XCTest
@testable import Communication_Semantics

final class Communication_SemanticsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Communication_Semantics().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
